version: '3.8'

services:
  web:
    build: .
    ports:
      - "8080:8080"
    environment:
      - ENVIRONMENT=production
      - PORT=8080
    volumes:
      - ./data:/app/data
    restart: unless-stopped
    mem_limit: 2g
    cpus: 1.5
